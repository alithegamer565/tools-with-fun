import dbConnect from '../../../lib/db'; import Admin from '../../../models/Admin'; import bcrypt from 'bcryptjs';
export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).json({error:'method not allowed'}); await dbConnect(); const { username,password } = req.body; if(!username||!password) return res.status(400).json({error:'missing'}); const exists = await Admin.findOne({}); if(exists) return res.status(400).json({error:'admin exists'}); const hash = bcrypt.hashSync(password,10); await Admin.create({username, passwordHash:hash}); return res.status(200).json({ok:true}); }
