import { useState } from 'react'; export default function Login(){ const [u,setU]=useState(''); const [p,setP]=useState(''); const [m,setM]=useState(''); async function login(){ const res = await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})}); if(res.ok) window.location='/admin/dashboard'; else { const j=await res.json(); setM('Error: '+j.error) } } return <div style={{padding:20,maxWidth:600,margin:'30px auto'}}><h2>Admin Login</h2><input placeholder='username' value={u} onChange={e=>setU(e.target.value)} /><br/><input placeholder='password' type='password' value={p} onChange={e=>setP(e.target.value)} /><br/><button onClick={login} className='btn'>Login</button><div style={{marginTop:12}}>{m}</div></div> }
