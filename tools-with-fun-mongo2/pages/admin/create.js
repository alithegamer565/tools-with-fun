import { useState } from 'react'; export default function Create(){ const [u,setU]=useState(''); const [p,setP]=useState(''); const [m,setM]=useState(''); async function create(){ const res = await fetch('/api/auth/create-admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})}); const j=await res.json(); if(res.ok) setM('Admin created. Now login.'); else setM('Error: '+j.error) } return <div style={{padding:20,maxWidth:600,margin:'30px auto'}}><h2>Create Admin</h2><input placeholder='username' value={u} onChange={e=>setU(e.target.value)} /><br/><input placeholder='password' type='password' value={p} onChange={e=>setP(e.target.value)} /><br/><button onClick={create} className='btn'>Create</button><div style={{marginTop:12}}>{m}</div></div> }
